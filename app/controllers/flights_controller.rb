class FlightsController < ApplicationController
  def index
    # @search_date = Date.new(params[])
    # @search_results = 
  end

  private

  # def flight_params
    # params.require(:flight).permit(:origin_airport_id, :destination_airport_id, :start, :duration)
  # end
end
