<section class="section">
  <h2 class="title is-2">Booking Successful</h2>

  <div class="box">
    <h6 class="title is-6">Flight Details</h6>
      <%= @booking.flight.pretty_departure_date %><br>
      <%= @booking.flight.info %>
    <div class="field">
    <h6 class="label is-6">Passengers</h6>
    <% @booking.passengers.each do |passenger| %>
      <%= passenger.name %><br>
      <%= passenger.email %><br>
    <% end %>
    </div>
  <div>
</section>